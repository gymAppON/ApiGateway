server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: userManager
          uri: http://localhost:8090
          predicates:
            #- Path=/users/**
            - Path=/auth/**